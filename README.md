# Expense-Tracker
A desktop-based Expense Tracker built using Java Swing for the user interface and SQLite (via JDBC) for data storage. This application allows users to register, log in, and manage their daily expenses by category with an intuitive GUI. It demonstrates a clean MVC architecture, database integration, and practical CRUD operations.
